import { PlaceholderPanel } from "../../../shared/components/PlaceholderPanel";

export function ClientBookPage() {
  return (
    <PlaceholderPanel
      eyebrow="Cliente"
      title="Solicitar turno"
      description="Placeholder del flujo de reserva para elegir servicio, fecha, horario y confirmar solicitud."
    />
  );
}
